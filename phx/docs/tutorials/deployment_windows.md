[Back to main README](../../README.md#deployment)

# Deployment on a Windows machine

This process is nearly identical to the [Deployment on Linux](./deployment_linux.md).

The only difference is how you would run the `start` script - please note that this command
must be executed using PowerShell:

```
.\scripts\start.ps1
```
